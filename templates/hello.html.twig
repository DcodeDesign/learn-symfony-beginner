{{ include('header.html.twig', sandboxed = true) }}

<div> {{title | upper }} </div>

{# Un commentaire #}

<ul>
    {% for item in array %}
        <li>{{ item }}</li>
    {% endfor %}
</ul>

{{ include('footer.html.twig', sandboxed = true) }}
